%p#notice= notice
%h1 Listing Installers
%table
  %thead
    %tr
      %th Employee key
      %th First name
      %th Last name
      %th{:colspan => "3"}
  %tbody
    - @installers.each do |installer|
      %tr
        %td= installer.employee_key
        %td= installer.first_name
        %td= installer.last_name
        %td= link_to 'Show', installer
        %td= link_to 'Edit', edit_installer_path(installer)
        %td= link_to 'Destroy', installer, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Installer', new_installer_path
